# AUTOPILOT STATE — Aquatycoon

STATUS: OK
Last run: 2026-08-28 (iter 62 — CONSTRUCTION-BUILDER terrain foundation cost — basin excavation cost varies with deterministic pseudo-heightmap per tile (0.92–1.18 multiplier, avg over footprint) + live cost preview & inspector breakdown; TerrainFoundation pure domain: terrainFactorForTile/ForRect/estimateBasinCAPEXWithTerrain/basinFoundationBreakdown/foundationConditionLabel/Tone/Pct/Summary with 32-bit hash, 0.92–1.18 band; GameManager basin CAPEX now terrain-adjusted (place/demolish/bulk/duplicate/stamp/updateDepth/Rect all use adjusted cost, stamp spiral now terrain-aware with insufficient-funds tracking); BasinInspector shows emerald/sky/amber/rose foundation pill + base×factor→adjusted with delta surcharge/discount + depth delta terrain-aware; App draw_basin live ghost preview shows Basin W×H · dimensions · $adjusted (condition pct) with valid/blocked hint + toast includes condition & base×factor; 16 TF tests TF01-12 pin determinism, bounds 0.92–1.18, variance, rect average, adjusted=round(base*factor), labels/tones, breakdown, GameManager place/demolish/depth/rect/move & template stamp terrain variance; B7/B8/B17/BE06h updated to terrain-adjusted; gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 62/62 ✅ eng 368/368 ✅)

Gate policy: `npm run build` + `npx tsc --noEmit` must be clean; suites:
`npm test` (sim), `npm run test:ui` (= static ui-tests + ui-interaction-tests),
`npm run test:eng`.

## ⭐ MISSION DIRECTIVE v3 (user, 2026-08-27) — SUPREME PRIORITY — SUPERSEDES v2
"BUILD THE PROCESS, DO NOT SELECT THE PROCESS." Full prompt parked in user message;
authoritative summary:

The player must PHYSICALLY BUILD the plant in the 3D world — basins, walls,
baffles, pumps, diffusers, mixers, membranes, media, blowers, pipes, valves,
electrical panels, cables, sensors, recycle loops — and the treatment process
emerges from those components. Technology = EMERGENT CONFIGURATION, not a dropdown.
Retire UnitDesigner as the PRIMARY design UX (keep it as Inspect/Diagnostics only).
70–80% of effort goes to direct manipulation / 3D construction / placement /
snapping / equipment visuals / connection tools / editing / demolition / emergent
process visualization. Do NOT disappear into backend equation work.

IMPLEMENTATION PHASES (build top-down, ONE coherent slice per iteration):
- PHASE 1 ✅ DONE (this run): ConstructionToolbar STRUCTURES→Basin; click-drag
  rectangular basin drawing; live ghost + dims + cost preview; valid/invalid
  coloring; basin 3D render (floor+walls+water); selection + demolition +
  volume-based cost; legacy units block basins & vice-versa. Tests: 19 basin
  domain tests in sim-tests.ts.
- PHASE 2 ✅ DONE (iter 25): physical equipment placement (diffuser, mixer, pump, blower) with
  mounting rules + 3D models + select/delete. 23 tests E1–E23.
- PHASE 3 ✅ DONE (iter 26): utility connections (water pipe, air pipe, power cable) —
  domain model + validation (type-specific host rules) + quantity CAPEX
  (water $185/m+900, air $110/m+600, power $65/m+400) + GameState
  `utilityConnections[]` + GameManager place/demolish (campaign 60% salvage,
  sandbox $0, cascade on basin/equipment removal) + SceneManager
  syncUtilityConnections (color-coded lines + endpoint dots + selection
  highlight + dashed preview) + toolbar UTILITY group (Water/Air/Power) +
  App connect_utility two-click flow (host-only endpoints, duplicate/length
  guards, cost toasts) + select/demolish/undo support. 23 new domain tests U1–U23.
- PHASE 4 ✅ SLICE 1 DONE (iter 27): **construction network — power & aeration live status**.
  NEW `src/design/ConstructionNetwork.ts` pure domain: `poweredEquipmentIds`
  (powerKw>0 needs incident power_cable, passive diffuser always live),
  `aeratedDiffuserIds` (air_pipe diffuser↔blower only when blower powered),
  `constructionStats` (basin volume/area, powered/aerated counts, live kW/opex).
  `GameManager` wrappers. `SceneManager.syncEquipment` now tints: unpowered
  machines dim red, aerated diffusers blue shimmer, idle diffusers dark (amber
  selection still wins). App: `ConstructionStatusChip` HUD (basins · powered ·
  aerated · utility lines) + `EquipmentInspector` (live status + power/aeration
  hint + remove) + select-mode toast shows live state + all syncEquipment
  call-sites pass powered/aerated sets + undo/level-load + Esc clearing.
  11 new domain tests N1–N9.
- PHASE 4 ✅ SLICE 2 DONE (iter 28): **thin adapter — live sim effects.**
  NEW `src/design/ConstructionAdapter.ts` pure domain: `evaluateConstructionEffects`
  (powered/aerated/mixer sets → bod/tn/tss/cod multipliers + DO boost + septic
  basins + livePower/liveOpex). Each aerated diffuser ≈3% BOD, 1.5% TN/TSS;
  basin volume adds up to 3–4% settling credit; basins without a powered mixer
  are septic (BOD+8% / DO−0.4 per dead zone, capped). `GameManager.tick`
  applies the adapter AFTER the legacy pipe/unit solver: effluent multiplied,
  power/OPEX added (0.15 $/kWh + OPEX split 40/60) and permit compliance
  re-evaluated (septic warning `construction_septic` surfaces via activeAlerts).
  Zero construction = zero effect (100% backward compat). 13 new CA tests
  (CA0 identity, CA1 aerated polish+BOD/DO+power, CA2 unaerated, CA3 septic vs
  healthy, CA4 power gate, CA5 tick septic alert lifecycle, CA6 aeration cap).
  Remaining Phase 4 work: CLOSED — adapter is Phase 4 final slice.
- PHASE 5 ✅ SLICE 1 DONE (iter 29): **zones & baffles — basin compartmentalisation (slice 1: placement + visuals).**
  NEW `src/design/BasinZone.ts` pure domain: `BaffleWall` (vertical/horizontal full-span interior walls, offset 1..dim-1, validate + $55/m²+450 CAPEX), `BasinZone` derived grid (sorted V/H offsets → Cartesian product cells, default roles anoxic/aerobic/buffer), `zonesForBasin`/`allZones`/`basinZoneStats`/`pointNearBaffle`/`baffleSummaryLine`.
  `GameState` gains `customBaffles[]` + `selectedBaffleId`; `GameManager.placeBaffle`/`demolishBaffle` (campaign 60% salvage, sandbox $0, duplicate/offset guards, cash-gated), `zonesForBasin`/`allZones`/`zoneAtTile`/`baffleAtPoint`/`zoneForEquipmentItem`/`basinZoneStats`, basin demolish cascades baffles with salvage.
  `SceneManager.syncBaffles` + `setBafflePreview` (thin concrete wall 0.18 m, violet #c4b5fd, purple emissive on select + dashed lilac preview line) mirrors syncUnits discipline.
  `BuildToolbar` new BAFFLES group (│ Vertical / ─ Horizontal, violet active) + `ToolMode 'draw_baffle'`. App: one-click `draw_baffle` inside basin (offset = tile - basin origin +1, ghost tile validity + dashed preview), select/inspect (violet highlight + toast with zone count/length) / demolish (60% refund) baffle-first hit-testing, Esc cancels baffle, undo/redo + level-load sync, basin inspect now shows zone count, demolish cascade renders.
  `ConstructionStatusChip` now shows zones/baffles chip (violet). NEW `BaffleInspector` panel (orientation, length, cost/salvage, remove). 19 new domain tests Z1–Z11 (single zone, vertical/horizontal split, 2V+1H grid, validation bounds/duplicate, CAPEX determinism, allZones, stats, pointNear, GameManager placement/funds/duplicate/zones/baffleAtPoint/demolish cascade, equipment zone membership).
  Remaining Phase 5 work: ✅ CLOSED — Phase 5 COMPLETE.
  ConstructionAdapter now reads derived zones: per-zone septic scales 1.045/zone (vs 1.08/basin fallback) with zone-aware alerts and UI.
  Equipment inspector shows host zone (role chip) and basin toast shows mixed/septic breakdown.
- PHASE 6 ✅ SLICE 1 DONE (iter 31): **membranes & carrier media — in-basin filtration kit (placement + 3D visuals).**
  NEW catalog entries `membrane_cassette` ($18.5k/5 kW, powered hollow-fiber cassette with suction power, needs cable + baffle zone context) + `mbbr_carrier` ($6.8k passive carrier wheels, fluidized by mixing) — both `in_basin` (must sit inside a drawn basin, one per tile, blocked by same rules as diffusers). `ConstructionNetwork` extended: `totalMembranes/poweredMembranes/totalCarriers` + `livePower` includes 5 kW cassettes; `constructionStats` + `constructionSummaryLine` surface membrane/carrier counts. `SceneManager.buildEquipmentMesh` adds two distinctive meshes: white vertical cassette housing + 5 fiber columns + header/stub for membranes, and a 7-carrier sky-blue wheel heap + faint wireframe cage at mid-depth scaled to basin depth for media. `BuildToolbar` EQUIP_ICONS adds ShieldCheck/Hexagon; palette auto-expands to 6 buttons. `EquipmentInspector` adds icon mapping + filtration-specific live-status copy (powered filtration vs passive fluidized) and retains zone chip. `ConstructionStatusChip` new cyan chip shows `x/y membranes` + `n carrier(s)` when any exist. 15 new domain tests FM1–FM15 pin mounting (in_basin vs ground), tile exclusivity, exact campaign CAPEX ($18.5k/$6.8k) + cash gate, unaffordable rejection, powered model (membrane needs cable — empty basin tile powers it alone so 5 kW isolated, carrier always live), stats power accounting, per-zone baffled membership (vertical baffle splits membrane vs carrier into gridI 0/1), 70% salvage on demolish, and basin integrity (membrane blocks basin demolish).
  Remaining Phase 6 work: ✅ CLOSED — Phase 6 COMPLETE: per-zone membrane TSS (live 0.20× / degraded 0.55×, floor 0.02) + carrier BOD (0.965× active, aerated extra 0.97× + TN 0.985×) wired via evaluateConstructionEffects with zone-scoped health/aeration maps; filtrationLiveSets() exposes live/degraded/active/aerated ids for 3D tints; SceneManager.syncEquipment now tints membranes cyan (live) / amber (fouled) and carriers cyan/sky/dim per filtration state; EquipmentInspector shows live/fouled/aerated copy + zone role; App passes filtrationLiveSets to all syncEquipment call-sites; GameManager.tick applies filtration multipliers after aeration/volume and before septic, with TP/turbidity following TSS; 13 new FM-tests pin healthy vs septic membrane TSS, unpowered identity, carrier active vs dormant, aerated extra BOD/TN, multi-membrane/carrier stacking with floors, baffled per-zone live vs dormant, degraded set, and tick TSS/BOD lifecycles.
- PHASE 7 ✅ SLICE 1 DONE (iter 33): **emergent process recognition — descriptive badges (read-only lens).** NEW `src/design/ProcessRecognition.ts` pure domain `recognizeProcess(basins,baffles,equipment,utilities) → ProcessBadge[]` (tones emerald/sky/cyan/violet/amber) emits up to 9 badge types from the live construction network — Compartmentalised (baffles→zones), Aerated (aerated diffusers), Mixed (powered mixers), Activated-sludge-like (aerated+mixed co-located), Anoxic→Aerobic (baffled train with one aerated + one non-aerated zone), Membrane barrier / MBR-like (powered cassettes live vs fouled), Biofilm media / MBBR-like (fluidised vs dormant), Hybrid IFAS-like (aerated carriers in same zone), Septic risk (unmixed zones). NEW `src/ui/ProcessBadgeStrip.tsx` HUD strip at top 92 px (violet/cyan/sky/emerald/amber pills with icons, 11px mono) rendered beneath the ConstructionStatusChip — second glance after the stats. `App.tsx` wires `recognizeProcess(...)` into the HUD. 16 new domain tests PR01–PR16 pin empty/identity, aerated/mixed/activated, compartment count, anoxic→aerobic vs fully-aerated, membrane live vs fouled+unpowered, biofilm active vs dormant, IFAS hybrid, septic incomplete, and summary line.
- PHASE 7 ✅ SLICE 2 DONE (iter 34): **instrumentation kit — process sensors (DO probe + flow meter + level transmitter).** Extended `ProcessEquipment` catalog with 3 sensor types: `do_probe` ($3.2k/0.3 kW, in_basin), `flow_meter` ($7.5k/0.45 kW, ground), `level_sensor` ($4.8k/0.25 kW, in_basin) — each occupies one tile, tile-exclusive, dry/wet mounting rules mirror diffusers/pumps, 70% salvage, cash-gated. `ConstructionNetwork` now tracks `totalSensors/poweredSensors` + per-type DO/flow/level + livePower/liveOpex + `constructionSummaryLine` telemetry. `SceneManager.buildEquipmentMesh` adds 3 distinctive meshes (yellow-tip DO probe shaft+transmitter, flanged mag-flow spool with teal box+glass, pole+horn ultrasonic with housing) scaled to basin depth, and `syncEquipment` tints sensors teal when live vs red when dark. `BuildToolbar` EQUIP_ICONS adds Activity/Gauge/Ruler (toolbar now 9 EQUIP buttons). `ConstructionStatusChip` adds teal/amber sensor chip and `EquipmentInspector` shows live/dark telemetry copy. `ProcessRecognition` gains 10th badge `Instrumented` (slate, ≥2 powered sensors; `full triad (DO+flow+level)` when all 3 types live) — observability without physics. 16 new IN-tests IN01–IN16 pin mounting, exclusivity, CAPEX, powered model, livePower/OPEX, basin integrity, Instrumented thresholds/triad/unpowered, salvage, summary and baffled zones. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- PHASE 7 ✅ SLICE 3 DONE (iter 35): **chemical dosing kit — TP polishing (storage + dosing pump).** Extended `ProcessEquipment` with `chemical_storage_tank` ($11.5k/0.6 kW ground) + `chemical_dosing_pump` ($6.8k/0.9 kW in_basin) — wet/dry mounting mirrors diffusers/pumps, tile-exclusive, 70% salvage, cash-gated; `ConstructionNetwork` now tracks `totalChemicalUnits/poweredChemicalUnits` per-type storage/dosing + livePower/OPEX + summary line; `ConstructionAdapter` adds TP polish: powered dosing pump in healthy (mixed) zone = 0.78× TP each, powered storage = 0.92× each, stacking capped 0.35 (TP only when basins exist); `chemicalLiveSets()` exposes poweredStorage/activeDosing sets for 3D tints; `SceneManager` renders 2 distinctive meshes (bunded horizontal chemical tank + kiosk, dosing skid day-tank + lime pump + lance) with lime shimmer when powered/active vs red/amamber when dormant; `BuildToolbar` adds FlaskConical/Beaker (toolbar now 11 EQUIP buttons); `ConstructionStatusChip` adds lime sensor chip; `EquipmentInspector` shows lime/dark chemical copy + zone chip; `ProcessRecognition` adds 11th badge `Chemically dosed` (lime, dosing active in mixed zone) / `Chemical ready` (amber) / dormant — chemical observability with live TP physics. 16 new CH-tests CH01–CH16 pin mounting, exclusivity, CAPEX, powered model, livePower, basin integrity, badge thresholds, TP multipliers (0.78/0.92/stack/cap), tick lifecycle, salvage, summary and baffled zones. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
  - iter 37 (2026-08-27): **TYCOON POLISH — reagent OPEX / flow-scaled chemical consumption (tycoon economic pressure).** NEW `src/design/ConstructionAdapter.ts`: `CHEMICAL_DOSE_MG_PER_L=60`, `CHEMICAL_REAGENT_PRICE_PER_KG=0.55`, `CHEMICAL_REAGENT_COST_PER_M3_PER_PUMP=0.033` ($0.033/m³ per active dosing pump). At L1 (3500 m³/d) one active pump ≈ $115/d; at L4 (12000 m³/d) ≈ $396/d. `chemicalReagentOpexPerDay(activePumps, flow)` pure domain + `reagentOpexPerDay` field on `ConstructionTickEffect` (flow-gated, only when basins>0 && activeDosing>0 && flow>10). `evaluateConstructionEffects` now takes optional 5th `flowM3d` param (default 0 keeps backward compat, 0→$0). Summary adds `$X/d reagent` chip when live. `GameManager.tick` now passes `finalEffluent.flowRate` into the adapter and charges `reagentOpexPerDay` as 100% chemical OPEX (`dailyChemicalCost += reagent`, `dailyOpex += reagent`) on top of static 40/60 split — dosing in septic zones or unpowered rigs costs nothing and yields no TP. `EquipmentInspector` for dosing pumps now shows live reagent line (`Reagent $X/d at Y m³/d (60 mg/L × $0.55/kg) — scales with flow & pumps`) or pre-flow hint, fed via new `flowM3d` prop from `App.tsx`. 14 new domain tests RE01–RE08 pin pure cost linearity (0/1/2 pumps, 0 flow, 3500 vs 12000), evaluate with/without flow, dormant septic, storage-alone free, baffled per-zone counting, summary inclusion, and tick financial lifecycle (chemical+opex uplift ≈ reagent, netProfit reduced). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 36 (2026-08-27): **TYCOON POLISH — owner-builder construction contracts (tycoon economy pressure).** NEW campaign objective engine: 4 flow-INDEPENDENT construction contract ids (`obj_custom_basins` basins≥target, `obj_custom_baffles` baffles≥target, `obj_custom_equipment` machines≥target, `obj_custom_powered` poweredCount≥target) — checked BEFORE the `hasEffluentFlow` gate so players can earn tycoon progress by building even before the plant flows, teaching “build first, then treat”. Handlers live in `GameManager.tick` (powered check reuses `_poweredIds` single source of truth; passive diffusers always count as powered). `LevelsData` L4 (Emerald Lake Eco-City, BNR stage) gains showcase contract `obj_custom_basins` target 2 — narrative: excavate 2 custom basins (or baffled zones) for staged anoxic→aerobic BNR, proving you can build the process instead of placing prefab A2O. Campaign completion now respects construction contracts (all-met latches). 8 new domain tests CC01–CC08 pin 0/1/2 basins, default target, baffles 2, equipment 3, powered 0/1/2 (passive diffuser counts), flow-independence (no-pipe plant still passes basins), Level 4 objective presence, and completion latching (construction alone insufficient until BOD also met). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
Remaining Phase 7 work: ✅ CLOSED — Phase 7 (construction-builder kit) COMPLETE. V2-B stage dressing landed iter 38 (biome-illustrated LevelModal). RO SLICE 1 landed iter 40, SLICE 2 landed iter 41 (live physics) — Next = RO SLICE 3 (brine pressure) or tycoon reputation/events.
Architecture: clean component model (CustomBasin/ProcessEquipment/
UtilityConnection) in src/design/*; adapter layer feeds existing sim; legacy
units stay as QUICK-BUILD until custom builder is primary, then become
PREFABS (editable placed pieces), never immutable magic units.

## ⭐ MISSION DIRECTIVE v2 (user, 2026-08-26 evening) — now SUBORDINATE to v3
User's long-term direction. v3's "fully designable units" maps onto v2-A; the
construction-builder is the NEW primary expression of it. Continue v2-B (story/
art/stages) and v2-C (launcher) alongside.

## ⭐ MISSION DIRECTIVE (user, 2026-08-26)
`MISSION_REDESIGN.md` (repo root) is the authoritative roadmap: full
architectural redesign into a wastewater-engineering tycoon / process-design
simulator. Work it TOP-DOWN:
1. ✅ Legacy backlog #1 cleared in iter 1.
2. Section A bug fixes: A1 terrain decals ✅, A2 tool invariant ✅, A3 permit
   single-source verification ✅, A4 CI gating ✅, A5 UI interaction tests ✅
   (landed iter 2), A6 PFD branching ✅, A7 junk cleanup ✅.
3. Then §AK PHASE 1 vertical slice: AUDIT each of the 17 items against the
   actual code, finish/strengthen what is partial, one coherent slice per
   iteration, tests per §AM.
4. After Phase 1 stabilizes: migrate MBR/RO/A2O/DAF/sludge/disinfection.
Never regress §AL. Respect §AI performance limits. User grants freedom on
front-end and back-end improvements beyond the mission after Phase 1.

## Iteration log
- iter 17 (2026-08-26): §AK items 5/6 CLOSED — ONE authoritative peak-flow
  basis everywhere. (a) NEW PeakFlow.VALIDATOR_REFERENCE_FLOW_M3D = 3500 (the
  validator's design basis replaces magic 5000 in DesignValidator; fresh CAS +
  clarifier carry zero peak issues). (b) Clarifier.ts legacy private
  PEAK_FACTOR = 1.8 deleted → imports shared PEAK_FLOW_FACTOR (×1.446); peakSor
  now honestly reports 19.9 m/d where it said 24.8 @L1. No runtime physics
  consumed that field, so zero sim impact. 4 new eng tests PF19–PF22 pin the
  shared constant, clean fresh-template validation for CAS+clarifier, and the
  death of ×1.8. Branch hygiene: main fast-forwarded to the autopilot-attempts
  tip (iters 15–16 had been stranded there); iteration committed on main.
  Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 26/26 ✅ eng 298/298 ✅.
- iter 18 (2026-08-26): Updated Level 1 briefing to reference VALIDATOR_REFERENCE_FLOW_M3D design basis. Gates: build ✅ tsc ✅ sim ✅ ui ✅.
- iter 19 (2026-08-26): §AK items 16/17 CLOSED — campaign L1/L2/L3 levels updated with new objectives (obj_pump, obj_cas_sizing) and Test S revised for staged pump+UV build. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui ✅.
- iter 19 (2026-08-26): §AK item 13 ✅ Show Calculation panel rolled out across all four engineerable unit types (CAS, clarifier, equalization basin, pump station) — each DiagnosticsTab now renders per‑process CalcBlock derivations with honest substituted equations and live numbers. §AK items 16/17 remain: updated campaign L1/L2/L3 levels with tests.
- iter 20 (2026-08-26): Wired per-contract design flow through placement context into DesignValidator. Added `OperatorControls.designFlowM3d?`; `estimateDesignFlow` now returns `unit.blueprint?.controls?.designFlowM3d ?? VALIDATOR_REFERENCE_FLOW_M3D`, retiring the Phase-1 heuristic's stated exit condition (§AK item 17 follow-up). Backward-compatible: absent contract flow falls back to the shared basis. New eng tests PF23/PF23b pin the override (8000 m³/d → CAS reports `blower_undersized`) and the clean fallback. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70+52 ✅ eng 300/300 ✅. New ui‑interaction-tests.tsx section added verifying every CalcBlock's equation string and EconomicsTab civil‑derivation math.
- iter 21 (2026-08-26): MBR migration SLICE 1 — real membrane design basis (Mission §Q+§R), replacing the legacy binary fouled/not-fouled. NEW `src/sim/processes/MBR.ts`: flux/area formulas (J=Qp·1000/(Am·24), Am=Qp·1000/(24·J)), clean + fouling/scour-adjusted design-basis TMP (TMP0=J/permeability, TMP=TMP0·(1+kF·foulLoad)), flux classification bands (≤18/≤25/≤30 LMH), and a §R membrane material catalog (PVDF/PES/ceramic) with comparative engineering attributes — no '+10% better'. `UnitBlueprint`: `MembraneDesign` equipment layer + `DEFAULT_MEMBRANE_DESIGN` (9×850 m² PVDF @20 LMH, adequate scour) + `blueprintFromTemplate('mbr_membrane')` wired with legacy 1728 m³ geometry. `Geometry.defaultGeometryFor('mbr_membrane')` added. `DesignValidator`: new `mbr_membrane` case emits engineering warnings — flux class (conservative→normal→aggressive/critical), TMP headroom vs material rating, sub-minimum air-scour. 10 new eng tests MBR01–10 (317/317); fresh template validates CLEAN. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 26/26+52/52 ✅ eng 317/317 ✅.

## Backlog (work top-down)
1. ✅ RESOLVED (iter 9): Fix Level-1 completion economy — SLR unit mismatch fixed; all 5 sim tests pass.
2. ✅ RESOLVED (iter 16): Fix sim Test K — TN/NH₄ violations under full diurnal peak. MBBR SND model added; compliance streak test passes.
3. Idea pool (post-mission freedom): blower VFD upgrade tree; sludge treatment line objectives; sound effects for placement.
4. ✅ RESOLVED (iter 10): Pump runout/service-factor clamping in `findPumpDutyPoint` — analytic intersections beyond the curve end now cap at runout (1.25×BEP default, affinity-scaled); `pump_at_runout` validator warning added.
5. ✅ RESOLVED (iter 11): EQ API returns `constituentMassKg` snapshot + `inflowM3d` in `EqStepResult`; validator EQ audit + live DiagnosticsTab readout landed with it (§AK item 10 telemetry half).
6. ✅ RESOLVED (iter 12): validatePipeVelocity wired into the PFD Pipe Engineering panel (live per-pipe warnings); §AK items 7/8 landed with it (auto‑sizing ladder, materials UI, cached hydraulics, CAPEX display).
7. ✅ RESOLVED (iter 13): Wired stepPumpStation into runtime pump_station (§AK item 9 runtime half). Pump stations now use real duty‑point solver (static lift + downstream headloss → system curve intersect), return PumpRuntimeTelemetry (status, duty point, power, BEP%, cavitation, failed units). VFD speedCommand honored, clog penalty stacks on duty‑point power/opex. Live telemetry panel in UnitDesigner DiagnosticsTab. 12 new PUMP_RT eng tests. All gates clean.
8. §AK Phase‑1 audit of remaining vertical‑slice items:
   - 1–3: design/runtime data model, custom geometry, CAS sizing (partial)
   - 5–6: blower/equipment capacity ✅, clarifier custom sizing ✅ — physics + validators + PF tests landed iter 15; iter 17 closed the residue by sharing ONE reference flow (VALIDATOR_REFERENCE_FLOW_M3D) between validator and template pins and purging Clarifier's private ×1.8 factor
   - 7–10: custom pipe diameter/material ✅, pipe headloss ✅ (iter 12), pump duty point ✅ runtime‑wired iter 13, equalization dynamic storage ✅ — §AK items 7–10 CLOSED
   - 11: quantity‑based CAPEX ✅ FULLY RESOLVED iter 14 (billing charged on build/edit/remove + 70% salvage; display ✅ iter 12)
   - 12: Unit Designer UI ✅ (enhanced §AK item 13 Show Calculation rollout)
   - 13: Show Calculation ✅ (rolled out across all four engineerable units in iter 19)
   - 14: dynamic influent ✅ (full strength 1.0 default since iter 15)
   - 15: engineering warnings ✅ phase 1+2 (+pipe velocity iter 12); membrane‑flux check lands with the MBR migration
   - 16–17: ✅ RESOLVED (iter 19): updated campaign L1/L2/L3 levels with new objectives (obj_pump, obj_cas_sizing) and revised Test S for staged pump+UV build.

Next goal (iter 63): **TYCOON sludge-energy circular balancing — biosolids fertilizer + CHP green-loop tuning OR RO brine zero-liquid deepening (brine handling → zero-liquid loop pressure); after that: tycoon reputation/events deepening or construction-builder polish (foundation visual cues + snap ergonomics).** Terrain foundation COMPLETE (per-tile hash 0.92–1.18, avg over footprint, live preview + inspector). Next = circular balancing or brine deepening.
RO migration COMPLETE — 3 SLICES + 5 dividends (reclaim + trust + seasonal + biosolids + green) + RANDOM EVENTS (storm/shock) + LIBRARY v1 landed. Tycoon depth: overdraft + reagent + brine + reclaim + trust + seasonal + biosolids + green + construction contracts + influent events + skid library; next = terrain foundation / sludge-energy balancing.
- iter 61 (2026-08-28): **CONSTRUCTION TEMPLATE LIBRARY v1 — reusable skid templates (Shift-select → save as named skid template + stamp anywhere).** NEW `src/design/ConstructionLibrary.ts` pure domain: `ConstructionTemplate{id,name,anchor,basins{dx,dy,w,h,depthM},baffles{orientation,offset,basinIndex},equipment{typeId,dx,dy,rotation}}` + `createTemplateFromSelection(sel,basins,equipment,baffles)` (empty/utilities-only rejected, anchor = min x/y, baffle re-parents via basinIndex, needs ≥1 basin/machine) + `estimateTemplateCAPEX` (sum basins+baffles+equipment) + `templateSummaryLine` + `validateTemplateStamp(anchor,[map],existingBasins,existingEquip,unitRects)` (bounds/overlap/equipment collision/in_basin vs ground mounting, baffle dim guards) + `stampTemplate(anchor)` (fresh ids basin _b/_bf/_eq + timestamp, re-parents baffle to new basin). `GameState` gains `constructionTemplates[]` (init []). `GameManager` adds `saveConstructionTemplate(sel,name)` (12-max guard, cash? save is free — stamp charges), `stampConstructionTemplate(tplId)` (spiral 8-tile search from hint min-basin for first valid anchor, cash-gated campaign CAPEX via estimateTemplateCAPEX, sandbox $0, fresh ids, newSelection for highlight), `demolishConstructionTemplate`. `BulkActionBar` adds indigo Save Template (BookmarkPlus) + `ConstructionLibraryStrip.tsx` (indigo Bookmark, per-template Stamp/Remove, cost pill, <Layers/Cpu/Columns3>). `App.tsx` adds `handleSaveTemplate` (name=Skid N + summaryLine, pushHistory, toast cost), `handleStampTemplate` (pushHistory, syncBasins/Baffles/Utility/Equipment/Brackets/DimensionLabels, amber handle, Stamp toast), `handleRemoveTemplate`. 17 new LIB tests LIB01-08 pin creation (empty/utilities-only, single basin dims+cost, twin-basin+diffuser+blower+baffle anchor/relative/cost), stamp validation (bounds/overlap/collision), stamping (dims+offsets+rotation+re-parent+fresh ids), GameManager save/stamp/salvage (sandbox $0, basins double, distinct location, demolish clears), error guards (empty, unknown, insufficient funds 101<cost, no-room on 6x6 full map), 12-max overflow and ground-template stamp. Gates: build ✅ tsc ✅ sim ALL PASS (LIB 17/17) ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 62 (2026-08-28): **CONSTRUCTION-BUILDER terrain foundation cost — basin excavation varies with deterministic ground (pseudo-heightmap per tile 0.92–1.18, avg over footprint).** NEW `src/design/TerrainFoundation.ts` pure domain: `terrainFactorForTile(x,y)` 32-bit hash 0.92–1.18, `terrainFactorForRect(rect)` avg, `estimateBasinCAPEXWithTerrain(basin)` Math.round(base*factor), `basinFoundationBreakdown` {base, factor, adjusted, delta, pct, label, tone, summary}, `foundationConditionLabel/Tone/Pct/Summary` (Soft 0.92–0.96 emerald, Average 0.96–1.02 sky, Firm 1.02–1.10 amber, Rocky 1.10–1.18 rose). `GameManager` basin CAPEX now terrain-adjusted everywhere: `placeCustomBasin`/`demolishCustomBasin`/`bulkDemolish`/`duplicateConstruction` (cost at new tiles)/`stampConstructionTemplate` (per-anchor terrain cost with cheapest-insufficient tracking)/`updateBasinDepth`/`updateBasinRect` (deltas terrain-aware). `BasinInspector` shows foundation pill (emerald/sky/amber/rose) with base×factor→adjusted + delta surcharge/discount + depth delta terrain-aware, installed cost now adjusted. `App.tsx` draw_basin live ghost preview hoverHint shows Basin W×H · dimensions · $adjusted (condition pct) with valid/blocked, toast includes condition & base×factor; initial basin instruction mentions terrain. 16 new TF tests TF01-12 pin determinism, bounds 0.920–1.180, variance >0.18, rect average = tile mean, adjusted=round(base*factor) in band, labels/tones, breakdown, GameManager place/demolish/depth/rect/move & stamp variance; updated B7/B8/B17/BE06h to terrain-adjusted + LIB07d insufficient-funds now tracks cheapest terrain cost. Gates: build ✅ tsc ✅ sim ALL PASS (TF 16/16) ✅ ui 62/62 ✅ eng 368/368 ✅.
- iter 60 (2026-08-28): **AOP OXIDATION — tertiary toxics + pathogen oxidizer (construction-built ozone-AOP skid).** NEW catalog `aop_skid` ($26.5k/9.5 kW ground, 70% salvage) — O₂ concentrator + ozone generator + reactor contactor, delivers ~88% pathogen kill + 55% toxics oxidation + 18% COD cut per powered skid when grid-connected via power_cable (industrial detox choice: stronger pathogens than UV 84% at 7.5kW, lighter than RO 92% at 12kW, and only construction kit besides RO that cuts toxics). `ConstructionNetwork` gains `totalAopUnits/poweredAopUnits/totalAopSkids/poweredAopSkids` + `livePowerKw` includes 9.5kW + `constructionSummaryLine` AOP chip; `ConstructionAdapter` adds `AOP_PATHOGENS_MULTIPLIER_PER_SKID=0.12/AOP_TOXIC_MULTIPLIER_PER_SKID=0.45/AOP_COD_MULTIPLIER_PER_SKID=0.82/AOP_BOD_MULTIPLIER_PER_SKID=0.90` + floors 0.002/0.08/0.45 + `aopMultipliers()` + `totalAopSkids/poweredAopSkids/liveAopSkids` gated `basins>0` + `pathogens/toxic/cod/bod multipliers` stacking (1x 0.12/0.45/0.82, 2x 0.0144/0.2025, 3x 0.0017/0.091, 4x toxic floor 0.08, multiplicative with RO+UV) and summary `N AOP skid(s) oxidizing/dormant`; `GameManager.tick` applies via generic `toxic/pathogens/cod/bod` multipliers (RO+UV+AOP stack multiplicatively, no extra tick code beyond adapter); `SceneManager.buildEquipmentMesh` adds O₂ tower + lime ozone cabinet + horizontal reactor vessel with yellow caps + vent stack + inlet/outlet stubs (distinct lime/amber palette) and `syncEquipment` tints AOP lime when powered else red; `BuildToolbar` adds Atom icon for AOP, `ConstructionStatusChip` adds lime/amber AOP pill (`x/y AOP live - toxics oxidation / needs Power cable`), `EquipmentInspector` adds AOP live (lime) / dark (amber) copy, `ProcessRecognition` adds 15th badge `AOP oxidation (live)` lime / `AOP (dormant)` amber. 16 new AOP tests AOP01-10 pin ground mounting+exclusivity+CAPEX+power model+stats+adapter gating (no-basin identity, unpowered identity, 0.12/0.45/0.82, 2x/3x/floor, helper, summary)+badges+basin integrity (ground AOP does NOT block basin demolish)+salvage+tick integration (toxics 73.7->33.2 55% cut, pathogens 0.12x, no-basin identity) with industrial-level 75 toxics fixture. Gates: build ✅ tsc ✅ sim ALL PASS (AOP 16/16) ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 59 (2026-08-28): **UV DISINFECTION — tertiary pathogen barrier (construction-built UV channel).** NEW catalog `uv_channel` ($21.5k/7.5 kW ground, 70% salvage) — open-channel low-pressure UV with 3 lamp racks, delivers ~84% pathogen kill per powered channel when grid-connected via power_cable (cheaper/lighter than RO's 92% at 12 kW: player chooses UV for pathogen-only polish vs RO for multi-barrier potable reuse). `ConstructionNetwork` gains `totalUvUnits/poweredUvUnits/totalUvChannels/poweredUvChannels` + `livePowerKw` includes 7.5kW + `constructionSummaryLine` UV chip; `ConstructionAdapter` adds `UV_PATHOGENS_MULTIPLIER_PER_CHANNEL=0.16` + `UV_PATHOGENS_FLOOR=0.003` + `uvPathogensMultiplier()` + `totalUvChannels/poweredUvChannels/liveUvChannels` gated `basins>0` + `pathogensMultiplier` stacking (1× 0.16, 2× 0.0256, 3× 0.0041, 4× floor 0.003, multiplicative with RO) and summary `N UV channel(s) disinfecting/dormant`; `GameManager.tick` already applies `pathogensMultiplier` generically (no extra code, RO+UV stack multiplicatively); `SceneManager.buildEquipmentMesh` adds concrete trough floor+walls + water hint + 3 stainless lamp racks (2 lamps each) + inlet/outlet stubs + cabinet violet lamp, and `syncEquipment` tints UV violet when powered else red; `BuildToolbar` adds Sun icon for UV, `ConstructionStatusChip` adds amber UV pill (`x/y UV live · pathogen barrier / needs Power cable`), `EquipmentInspector` adds UV live (violet) / dark (amber) copy, `ProcessRecognition` adds 14th badge `UV disinfection (live)` violet / `UV (dormant)` amber. 16 new UV tests UV01-10 pin ground mounting+exclusivity+CAPEX+power model+stats+adapter gating (no-basin identity, unpowered identity, 0.16/0.0256/0.004/ floor, helper, summary)+badges+basin integrity (ground UV does NOT block basin demolish)+salvage+tick integration (0.16× pathogens, no-basin identity, UV+RO stacking < either alone). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 62/62 ✅ eng 368/368 ✅.
- iter 58 (2026-08-28): **CONSTRUCTION-BUILDER ergonomics — edge-snap alignment guides + duplicate skid (builder ergonomics deepening).** NEW `src/design/SnapGuides.ts` pure domain: `SNAP_THRESHOLD_TILES=1`, `collectGuides()` (basin x/x+w, equipment x/x+1, unitRects), `snapTile()` (left/right edge candidates gx/gx-1), `snapHoverForBasin()` (right exclusive vs left edge direction-aware), `snapHandleTarget()` (n/s/e/w/ne/nw/se/sw). `SceneManager` gains `snapGuideGroup` + `setSnapGuides(vGuide,hGuide,mapSize)` (amber dashed LineDashedMaterial map-spanning at snapped edge, 0.06y, 950 renderOrder, DashedMaterial dash 0.35 gap 0.22) + `clearSnapGuides()`. `App.tsx` integrates snap on 4 live ghosts (draw_basin hover, place_equipment ghost, basin wall handle drag, equipment group drag) + 2 click commits (draw_basin second corner, place_equipment install) via `collectGuides` excluding self/group + `snapTile/snapHover/snapHandleTarget` before validation; `setSnapGuides/clearSnapGuides` wired on all pointerMove/pointerUp/cancel/hover-null paths + Esc. `GameManager.duplicateConstruction(state,sel)` pure domain: searches offsets (2,2) diagonal first then spiral ±6 for first valid global dx,dy that passes `validateBasinPlacement` for all basins, `validateEquipmentPlacement` for all equipment (host-basin check for in_basin kit), `validateBafflePlacement` for baffles (basin must be selected), atomically clones basins/equipment/baffles with fresh ids (`_copy` suffix dedup), cash-gated campaign cost sum CAPEX (basin+equipment+baffle), sandbox free, utilities skipped (toast rewire). `BulkActionBar` adds sky Duplicate button (Copy icon, sky-500) + `onDuplicate` prop; `App` gains `handleDuplicateSelection` (pushHistory, duplicate, sync all, select new skid, handle at new anchor, brackets+dimension labels, clearSnapGuides, toast with parts+cost) + `Ctrl+D` shortcut in select mode (reuses duplicateConstruction). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅
- iter 57 (2026-08-28): **TYCOON CHP — sludge→energy biogas engine (construction-built CHP closes sludge→energy circle).** NEW catalog `biogas_chp_skid` ($36.5k/1.8 kW parasitic, ground, 70% salvage) — containerized CHP burns digester biogas → 14 kW green per powered skid when grid-connected via power_cable and fed by an anaerobic_digester (+ basin + flow>10). `ConstructionNetwork` gains `totalChpUnits/poweredChpUnits/totalChpSkids/poweredChpSkids` + `livePowerKw` includes 1.8kW parasitic + `constructionSummaryLine` CHP chip; `ConstructionAdapter` adds `CHP_GREEN_KW_PER_SKID=14` + `chpGreenKw()` + `totalChpSkids/poweredChpSkids/liveChpSkids/extraGreenKw` gated basins+digester+flow+powered with `hasDigester` 8th param; `GameManager.tick` computes `hasDigesterForChp` from `simResult.updatedUnits`, passes to adapter, adds `extraGreenKw` to `totalGreenGenerationKw` and recomputes `energySelfSufficiencyPercent` (+ publicApproval) alongside existing demand/OPEX logic; `SceneManager.buildEquipmentMesh` adds green container + exhaust stack + radiator + lamp mesh for CHP and `syncEquipment` tints CHP emerald when powered else red; `BuildToolbar` adds Flame icon for CHP, `ConstructionStatusChip` adds emerald/amber CHP pill (`x/y CHP live · N kW green`), `EquipmentInspector` adds CHP live/dormant copy, `ProcessRecognition` adds 13th badge `Sludge→Energy (CHP live)` emerald / `CHP (dormant)` amber. 16 new CHP tests CHP01-10 pin ground mounting+exclusivity+CAPEX+power model+stats+adapter gating+summary+badges+basin integrity+salvage+tick integration (baseline 3→17 kW green). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 56 (2026-08-28): **CONSTRUCTION-BUILDER P5 slice 2 — multi-equipment group drag + hover cost polish (Shift block moves as one).** `GameManager` gains `moveEquipmentGroup(state, ids, anchorId, targetX, targetY)` (atomic, offsets dx=target-anchor preserved, utility-gated per member, group-aware validation: remainingEquipment excludes whole group + unitRects excludes group ground kit, inter-target duplicate reject, same-tile no-op). `App.tsx` `equipHandleDragRef` now `{anchorId, groupIds, anchorStart, typeId}` and `syncEquipHandleVisual` shows handle for any equipment selection (first selected as anchor); `onPointerDown` grabs any selected equipment tile as anchor with full `groupIds` (Shift block); `onPointerMove` group ghost: ddx/ddy delta, per-member `validateEquipmentPlacement` against remainingEquipment+unitRects, aggregated `$/kW` in hint (`3 machines · $52,700 · 26 kW`), inter-target collision guard, pan suppressed; hover affordance `onSelectedEquipTile` shows aggregated `N machines · $X · Y kW — drag as block` when ≥2 else lone hint, `hEquip` retains `· $X · Y kW` polish; pinned drag hint now `machines as block`; `onPointerUp` commits via `moveEquipmentGroup` when group>1 else `moveProcessEquipment` (pushHistory, utility re-check, `syncEquipment` with Set of groupIds, handle refresh at new anchor, `syncSelectionBrackets` group rects, toast aggregated block `N machines · $X · Y kW`); `EquipmentInspector` tip adds `Shift+Click to select multiple, then drag any handle to move the block as one ($/kW aggregated)`. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 55 (2026-08-28): **CONSTRUCTION-BUILDER P5 — In-world equipment drag handles + hover ghost polish (direct manipulation).** `SceneManager` gains `equipDragHandleGroup` + `syncEquipmentDragHandle(item|null, basins)` (amber box 0.32 m + stem+cone at `depth+0.55` for in-basin else `1.15` ground, emissive amber, castShadow) mirrors `syncBasinHandles` discipline + `equipDragHandleGroup` added to scene on `syncBasins`/`syncEquipment` paths. `App.tsx` gains `equipHandleDragRef` + `cancelEquipHandleDrag` + `syncEquipHandleVisual` (lone selected equipment → amber handle, else null via useEffect on `constructionSelection/processEquipment/customBasins`) + pointer handling: `onPointerDown` grabs equipment tile when lone equipment selected (no Shift, equipment>basin priority, forgiving 1×1 handle) before basin handle check, `onPointerMove` while `equipHandleDragRef` held shows live green/red ghost via `terrainGrid.setGhostPreview` (same validation as `moveProcessEquipment`: utility-attached red blocked, same-tile green, `validateEquipmentPlacement` self-excluded + ground vs basin mount + unitRects, pan suppressed) + hover affordance: lone-equip `▸ Drag X — hold amber handle and drag…` priority before basin wall, generic `hEquip` hint now `▸ Name · $X · Y kW — Click to inspect · Drag handle when selected · Move/Rotate` (cost/power polish), active drag pinned `↔ Dragging X — release…`; `onPointerUp` commits via `GameManager.moveProcessEquipment` (pushHistory, utility guard, no-op guard,  `syncEquipment/syncUtility/syncBaffles/syncBasins/syncEquipmentDragHandle/syncSelectionBrackets` + ghost clear + toast + undo) with priority alongside basin wall drag before `wasDrag` gate; `onPointerCancel`/`Esc`/`Right-click` cancel handle drag (Esc: equip handle before basin handle), `applyHistoryState`/`handleSelectLevel`/`clearConstructionSelection`/`Delete` bulk/`Esc` clear selection all call `syncEquipmentDragHandle(null)` ; `EquipmentInspector` tip adds amber drag-handle hint. Gates:...
- iter 54 (2026-08-28): **CONSTRUCTION-BUILDER P4 slice 3 — In-world multi-type grouping brackets + floating dimension labels (amber grouping cues + live m/m³ overlay).** `src/design/BasinZone.ts` gains `baffleRectFor(baffle,basin)→{x,y,w,h}|null` (0.8 m thin strip at tile boundary, vertical/horizontal, null on basinId mismatch) + `src/design/UtilityConnection.ts` gains `utilityRectFor(conn)→{x,y,w,h}` (bounding box over ax/ay→bx/by +1 tile, min 1×1 so thin H/V lines still bracket). `SceneManager` gains `dimensionLabelGroup` + `dimensionSpriteMap` + `syncDimensionLabels(basins,selectedIds)` (per selected basin: canvas 560×64 pill `rgba(15,23,42,0.88)` + amber border + mono text e.g. `18×12 m · 4.0 m deep · 864 m³`, Three.Sprite 5.2×0.62 depthTest false renderOrder 900, add/remove/dispose texture+material mirrors syncUnits, headless guard `document===undefined`, re-creates only when label/depth change, position `y=max(1,depth)+1.55`; `basinLabelText` + `makeDimensionSprite` helpers; `syncSelectionBrackets` leg clamped `Math.min(0.9, min(w,h)*0.45+0.15)` so 1×1 equipment & 0.8 m baffles don't overlap, material now `transparent opacity 0.95` + dispose lineMaterial) + bracketGroup already present. `App.tsx` gains `bracketRectsForSelection(sel,basins,baffles,utilities,equipment)→rect[]|null` (≥2 selection only; basins exact footprint, equipment 1×1, baffles via baffleRectFor, utilities via utilityRectFor) + `syncDimensionLabels` wired on 13 paths: init (syncBasins+syncDimensionLabels), create-basin, handle-drag commit, every Inspect `handleCanvasClick` branch (multi-select toggle, equipment/baffle/utility/basin single select, empty clear), bulkDemolish, Ctrl+A select-all, Esc/applyHistoryState/level-load clear, and BasinInspector edits (depth+rect). `syncSelectionBrackets` now fed `bracketRectsForSelection(...)` on every multi-select mutation instead of only basins, so Shift-grouped equipment/baffle/utility tiles show amber L-brackets. Headless tests untouched (document guard). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 53 (2026-08-28): **CONSTRUCTION-BUILDER P4 slice 2 — In-world basin wall drag-handles (3D handles + direct grip-resize + grouping brackets).** `src/design/CustomBasin.ts` gains `BasinHandleDir` + `basinHandleDirForTile(basin,tile)` (any perimeter tile → n/s/e/w/nw/ne/sw/se, corners priority, forgiving wall grab) + `basinRectForHandleDrag(basin,dir,targetTile)` (snaps dragged edge/corner to target tile, opposite edge fixed, caller validates via `validateBasinEdit`) + `basinHandleTiles` (8 midpoint/corner tiles for 3D handles). `SceneManager` gains `basinHandleGroup`/`bracketGroup` + `syncBasinHandles(basin|null)` (8 amber wall-top boxes 0.30–0.34 m at depth+0.12, corners amber brighter) + `syncSelectionBrackets(rects)` (corner L-brackets 0.9 m legs, amber LineSegments) wired inside `syncBasins` (single selected basin shows handles, multi-basin selection shows grouping brackets; mirrors syncUnits discipline). `App.tsx` gains `selectedBasinIdRef` + `basinHandleDragRef` + `cancelBasinHandleDrag`, pointer handling: `onPointerDown` grabs handle when lone basin selected and tile is perimeter (equipment/baffle not stolen), `onPointerMove` while handle held validates candidate via `validateBasinEdit` (map/basin/unit/equipment/baffle guards, bounds/min/max depth/strand/baffle checks) and shows green/red ghost via `terrainGrid.setGhostPreview` (pan suppressed), `onPointerUp` commits via `GameManager.updateBasinRect` (pushHistory, cash-gated 50% salvage, Insufficient-funds guard, no-op guard, syncBasins/Baffles/Utility/Equipment + ghost clear + toasts charged/refunded/dimensions), `onPointerCancel`/Esc/Right-click cancel, `applyHistoryState`/`handleSelectLevel` clear handle drag, hover affordance: handle `▸ Drag North wall…` priority, basin `Drag wall/corner to resize` vs `Drag handles when selected`, active drag pinned `↔ Dragging North wall…` hint. `BasinInspector` tip updated to mention wall-top amber handles. 8 new implicit guards exercised via live ghost validation; no new sim-tests required (existing BE08–BE09 pin validateBasinEdit parity). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 52 (2026-08-28): **CONSTRUCTION-BUILDER P4 slice 1 — Shift multi-select + bulk demolish + equipment grouping cues.** NEW `src/design/ConstructionSelection.ts` pure domain `ConstructionSelection{basins,equipment,baffles,utilities}` + `emptySelection/selectionCount/toggleSelection( set|add|toggle )/selectionSummaryLine/basinIdsSet` etc. `GameManager.bulkDemolish(state, sel)` atomic: validates explicit ids, auto-cascades interior equipment when basin selected (so Shift-dragged blocks clear in one click, unlike single-basin which rejects when occupied), also cascades baffles inside basins and utilities touching demolished basins/equipment; sums salvage 50% basins /70% kit /60% baffles&utilities (camp/sandbox aware, dedup explicit vs cascade). `SceneManager` `syncBasins/Equipment/Baffles/UtilityConnections` now accept `Set<string>|string|null` for multi-highlight (emerald basins, amber machines, white utilities, violet baffles all support Set). NEW `src/ui/BulkActionBar.tsx` (amber pill, BoxSelect, count+summary+salvage, Bulk Demolish + Clear, grouping cue pills emerald/orange/violet/sky, Shift+Click hint). `App.tsx` gains `constructionSelection` state+ref+clear helper, `handleCanvasClick` now carries `shiftKey/ctrlKey/metaKey` via `onPointerUp`, Inspect-mode click reworked: Shift/Ctrl toggles multi-select (equipment>baffle>utility>basin priority kept), bare click replaces, empty Shift preserves, Empty bare clears; highlights synced via Sets; inspectors gated `selectionCount<=1` while BulkActionBar shows when >1; `Ctrl+A` selects all construction in Inspect mode; `Esc` clears multi first; `Delete/Backspace` bulk-demolishes the whole selection (pushHistory, bulkDemolish, sync all, toast with counts+refund); `handleSelectLevel` also clears selection; `handleBulkDemolish` callback shared by bar. 14 new tests SM01+BM01-06 pin pure selection set/add/toggle/summary, basin 50%/sandbox 0%, pump 70%, basin+interior cascade (bonus sandbox $0, explicit dedup), baffle 60%+cascade, utility cascade via equipment, empty/unknown guards, multi-kind 1+1+1 batch. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 51 (2026-08-28): **CONSTRUCTION-BUILDER P3 — Builder prominence & selection ergonomics (toolbar grouping + hover hints + pick priority).** `src/ui/BuildToolbar.tsx` gains unmistakable `CONSTRUCTION BUILDER` amber banner (HardHat, gradient amber→orange, `BUILD THE PROCESS, DON'T SELECT IT` pill + live counts `# machines · 3 utilities · baffles`) plus grouped clusters with labelled borders — ACTIONS (Inspect/Pipes/Demolish+Rotate), STRUCTURE (Basin amber), EQUIPMENT (orange, 13 machines flex-wrap, ring highlight), UTILITIES (sky Water/Air/Power), BAFFLES (violet │/─) — and a collapsible muted `LEGACY · QUICK-BUILD PREFABS` heritage strip (dashed border, Package, `heritage — Inspect-only` badge, Chevron toggle, `showLegacy` state, category tabs + cards muted vs builder's saturated tones). `src/App.tsx` gains `hoverHint` state + `pointerMove` Inspect-mode contextual affordance (equipment>baffle>utility>basin>legacy priority, costs/volumes, `▸ Machine — Click to inspect · Move/Rotate`) with top-92px pill, plus construction-first select priority reorder (equipment>baffle>utility>basin>legacy unit) — legacy now LAST, basin toast now `Drag handles in panel to resize`, equipment toast retains live status. Pure UX (no sim/physics change). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 50 (2026-08-28): **CONSTRUCTION-BUILDER P2 — Equipment move/rotate (free reposition + visual yaw).** `src/design/ProcessEquipment.ts` gains `rotation?: 0|90|180|270` on `ProcessEquipmentItem` + `validateEquipmentPlacement(..., ignoreEquipmentId?)` self-excluded check. `GameManager` adds `moveProcessEquipment(state,id,newX,newY)` (free, same validation as placement self-excluded, blocked when any `utilityConnections` still attached to source tile, identical CAPEX, OK same-tile no-op) + `rotateProcessEquipment(state,id)` (90° clockwise, free, 1×1 so no footprint/validation, preserved across ticks). `SceneManager.syncEquipment` now takes `rotation?` and repositions + yaws meshes live when equipment moves/rotates (disposes old correctly, emerald/cyan/amber selection still wins). `App.tsx` gains `movingEquipmentId` armed state + `cancelEquipmentMove` + pointerMove ghost (green=valid red=blocked for moving type via same validation, utility-gated red, same-tile green) + `handleCanvasClickInner` priority handler (utility-gated guard toast, pushHistory, syncEquipment/syncUtility/syncBaffles, free) + Esc/RMB cancel + level-change/undo ghost clear. NEW `src/ui/EquipmentInspector.tsx` Move/Rotate row: Move toggles armed cyan-pulse `Click destination...` + hint, Rotate 90° button, rotation badge when non-zero. 15 new EM-tests EM01–EM09 pin in-basin vs ground mounting, occupancy, bounds, utility blocking + removal, unknown/same-tile, rotation cycle 0→90→180→270→0 + unknown + tick preservation. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 49 (2026-08-28): **CONSTRUCTION-BUILDER P1 — Basin direct editing (depth retune + footprint resize + in-world dimensions).** `src/design/CustomBasin.ts` gains `BASIN_MIN_DEPTH_M/BASIN_MAX_DEPTH_M/BASIN_DEPTH_STEP_M` + `validateBasinEdit(basinId,newRect,newDepth,map,basins,units,equip,baffles)` (self-excluded overlap, 1.5–8.0 m depth, stranded-equipment + baffle-validity guards, 2×2 min, bounds) + `basinEditCostDelta` + `basinDimensionLabel` (e.g. “18×18 m · 4.0 m deep · 1,296 m³”). `GameManager` adds `updateBasinDepth`/`updateBasinRect` (cash-gated campaign 50% salvage on shrink, sandbox $0, insufficient-funds guard, tutorial $0, delta via estimateBasinCAPEX). `SceneManager.syncBasins` now rebuilds mesh when w/h/depthM change (disposes old geometry, caches _basinW/H/Depth) + emerald emissive selection. NEW `src/ui/BasinInspector.tsx` (148 lines): depth slider 1.5–8.0 step 0.5 with live CAPEX delta preview + Apply, 8 cardinal resize buttons (North/South/East/West ±1 tile = 6 m, expand slate / shrink amber), dimensions banner (Ruler + tiles→meters→m²→m³ chips, CAPEX, zones, machines), 50% salvage demolish. `App.tsx` wires `selectedBasinId` → BasinInspector (pushHistory, update/demolish → syncBasins/syncBaffles/syncEquipment/syncUtilityConnections + toasts for charged/refunded). 17 new BE-tests BE01–BE10 pin pure guards (depth bounds, overlap self-excluded, stranded equipment, baffle validity, cost delta, label), GameManager depth/resize cash & salvage (deepen $57,420 / refund $28,710 50%, expand $73,920 / shrink $36,960, west shift, w=1 reject, no-op, unknown), guard rejections, insufficient funds, sandbox free, and tick lifecycle (depth 5 preserved, flow ~3447). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 48 (2026-08-28): **TYCOON RANDOM EVENTS — storm surge & industrial shock influent spikes (municipal sewer variability).** NEW `src/design/InfluentEvents.ts` pure domain: `InfluentEvent{storm_surge: 1.85× flow ×0.88 organics ×0.90 toxics for 1.4 d, industrial_shock: 1.0× flow ×1.65 organics ×2.2 toxics for 1.0 d}` + deterministic `pseudoRandomForDay(day)` hash (replayable, no Math.random, ~6.5% storm + 4.5% shock → ~11% event days ~51/yr) + `activeInfluentEventForDay(day)` (checks current & prior start so 1.4 d spillover) + `applyInfluentEvent(influent,event)` (flow + bod/cod/tss/tn/tp/pathogens/turbidity/toxics) + `influentEventSummaryLine`/`Label`. `GameManager.tick` now derives `influentEvent`/`eventInfluent` after diurnal (eventInfluent feeds `SimulationEngine.stepSimulation` instead of dynamicInfluent) and surfaces/clears `influent_event` warning alert (`Storm surge: +85% hydraulic load (6475 m³/d) — diluted ×0.88 but masses surge for 1.4 d!` vs `Industrial shock: BOD/COD ×1.65 organics + toxics ×2.2 (1.0 d) — equalization & aeration stressed!`, flow-gated, no alert when no flow). `HeaderHUD` new storm/shock pill (sky `🌧️ STORM SURGE` / amber `⚠️ ORGANIC SHOCK` alongside power/day, tooltip with multipliers, hidden when calm or no flow). 15 new IE-tests IE01–IE04 pin pure guards (NaN/Inf null, constants 1.85/0.88/1.65/ durations 1.4/1.0, hash range, deterministic, summary), identity vs storm vs shock apply (6475 flow / 193 BOD vs 363 BOD / 2.7 vs 6.6 toxics), schedule brute-force (storm 15 / shock 40 / calm 0, spillover +1.2 live / +1.5 ends, +0.5 shock live / +1.1 ends), and tick lifecycles (flat-diurnal minimal plant: storm 6475 >1.45× calm 3500 + storm alert / calm none, shock BOD 329 >1.3× calm 199 + shock alert, no-flow suppresses even on storm day). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 47 (2026-08-28): **TYCOON GREEN DIVIDEND — energy independence subsidy (city pays when green ≥50% self-sufficiency).** NEW `src/design/GreenDividend.ts` pure domain: `GREEN_DIVIDEND_RATE=0.08` / `THRESHOLD=50` + `greenDividendBonusPerDay(flow,tariff,selfPct)` (flow>10 & tariff>0 & self≥50, NaN/Infinity guards, flat 8% of tariff revenue) + `greenDividendLabel`/`SummaryLine` — At L1 (3500×$0.45) → $126/d, at L4 (12000×$0.95) → $912/d, at L5 (15000×$0.90) → $1080/d, stacks with all other dividends. `GameFinancials` gains `dailyGreenBonus` (0 in `SimulationEngine` + `createInitialState`). `GameManager.tick` computes bonus from `simResult.overallStats.energySelfSufficiencyPercent` + `finalEffluent.flowRate` + `tariffPerM3` via helper (self derived from CHP+solar+wind vs demand, 100% when demand 0 & green>0), adds to `dailyGreenBonus`/`dailyRevenue`/`netDailyProfit`, surfaces `green_bonus` success alert (`Green energy dividend: +$126/d subsidy (Energy independent — 83% self-sufficiency offsets grid!)`) and stacks with biosolids+seasonal. `HeaderHUD` emerald pill `+ $X green · Y% self` alongside biosolids/reclaim/trust/seasonal/financing, tooltip includes green, always visible when >0.5. 16 new GC-tests GC01–GC08 pin pure guards (0 flow/0 tariff/<50%/NaN/Inf), L1/L4/L5 scaling (126/912/1080), threshold 49 vs 50 vs 100, label, tick lifecycles (flowing wind+solar live 126.9 + alert, no-flow 0, grid-dependent 0% self no green), and stacking (green+bios+seasonal). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 45 (2026-08-28): **TYCOON SEASONAL — annual tariff cycle (drought premium / winter discount).** NEW `src/design/SeasonalProfile.ts` pure domain: `SEASONAL_TARIFF_AMPLITUDE=0.12` / `seasonalTariffMultiplier(days)=1+0.12·sin(2π·(days−80)/365)` (peak +12% day 171 summer drought, trough −12% day 353 winter low, equinox 0%) + `seasonalBonusPerDay(flow,tariff,days)=flow·tariff·(mul−1)` (guards flow>10 & tariff>0, NaN/Infinity→0) + `seasonalLabel`/`seasonalSummaryLine`. Flow-gated like trust/reclaim, no construction gate. `GameFinancials` gains `dailySeasonalBonus` (can be negative; 0 in `SimulationEngine` + `createInitialState`). `GameManager.tick` computes `seasonalBonus` from `newDays` (post-tick clock) via helper, adds to `dailySeasonalBonus`/`dailyRevenue`/`netDailyProfit`, surfaces `seasonal_bonus` alert (`Summer drought premium: +12% … scarcity lifts price to $+189/d` success vs `Winter tariff discount: −12% … surplus lowers price $−189/d` info) and stacks with reclaim+trust. `HeaderHUD` seasonal pill: amber `+ $X seasonal · Summer drought +12%` in summer, sky `− $X seasonal · Winter low −12%` in winter, alongside financing/reclaim/trust, tooltip includes seasonal, always visible when |bonus|>0.5. 16 new SE-tests SE01–SE08 pin pure mul/bonus/ guards (±12% range, L1/L4/L5 scaling), label, no-flow, tick lifecycles (171 vs 353 profit gap), stacking (seasonal+trust, seasonal+trust+reclaim), and overdraft interaction. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 46 (2026-08-28): **TYCOON SLUDGE CIRCULAR — biosolids fertilizer offtake (closed sludge loop dividend).** NEW `src/design/SludgeCircular.ts` pure domain: `SLUDGE_BONUS_PER_M3_THICKENER=0.012` / `DIGESTER=0.015` / `DEWATERING=0.012` / `FULL=0.039` + `sludgeCircularBonusPerDay(flow,hasThick,hasDig,hasDew)` (flow>10 & thickener-gated, digester requires thickener, dewatering requires digester, NaN/Infinity guards, ~$42/d thickener / $95 thick+dig / $137 full at L1 3500, $468 L4 12k, $585 L5 15k, tariff-independent fertilizer commodity) + `sludgeCircularLabel`/`sludgeCircularSummaryLine`. `GameFinancials` gains `dailyBiosolidsBonus` (0 in `SimulationEngine` + `createInitialState`). `GameManager.tick` computes bonus from `simResult.updatedUnits` presence (thickener/digester/dewatering||solar_drying) + `finalEffluent.flowRate` via helper, adds to `dailyBiosolidsBonus`/`dailyRevenue`/`netDailyProfit`, surfaces `biosolids_bonus` success alert (`Biosolids circular: +$42/d fertilizer offtake (Thickened — closed sludge loop adds value!)` with tier label Full circular / Thickened + digested / Thickened) and stacks with reclaim+trust+seasonal. `HeaderHUD` biosolids lime pill `+ $X biosolids` alongside reclaim/trust/seasonal/financing, tooltip includes biosolids, always visible when >0.5. 15 new SC-tests SC01–SC08 pin pure helper guards (0 flow/no-thickener/Nan), tier linearity (42→95→137), dewater without digester 0 extra, L4/L5 scaling, no-thickener 0, tick lifecycles (thickener $42 live + alert, no-flow 0, full 3× thickener profit gap, bios+seasonal stacked). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 44 (2026-08-28): **TYCOON TRUST — municipal trust dividend (reputation streak premium).** NEW `TRUST_BONUS_RATE_PER_DAY=0.015` / `MAX=0.12` min 2d in `GameManager.ts`: pure `trustBonusPerDay(streak,flow,tariff)` (floored whole days, flow>10 & tariff>0, NaN/Infinity guards) + `trustBonusRate` — 2d 3% ($47/d L1), 4d 6% ($94), 8d cap 12% ($189 L1 / $1368 L4 / $5400 L5, 10d same cap), 2.9d floors to 2d. `GameFinancials` gains `dailyTrustBonus` (0 in `SimulationEngine` + `createInitialState`). `GameManager.tick` now derives `complianceStreakDays` BEFORE cash flow (≥90 & flow>10), computes `trustBonus` via helper, adds to `dailyTrustBonus`/`dailyRevenue`/`netDailyProfit`, surfaces/clears `trust_bonus` success alert (`Municipal trust dividend: +$93/d … 6% × 4d streak`) and stacks with `reclaim_bonus`. `HeaderHUD` cash pill now shows violet `+ $X/d trust · Yd streak` alongside amber financing & cyan reclaim, tooltip includes trust, second line shows profit + trust + reclaim. 16 new TR-tests TR01–TR08 pin pure guards/linear/floor/cap/L4/L5 scaling, tick lifecycle (2d trust live $46.9, 8d cap $189, 8d >3.5× 2d, profit uplift, 0 streak), violation reset, no-flow, and trust+reclaim stacking (compliant train + 3 RO both live + alerts). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 43 (2026-08-28): **TYCOON DIVIDEND — reclaimed water tariff premium (15%/skid cap 36% on potable reuse TSS≤2/path≤2000/turb≤2/tox≤5; flow×tariff scaling L1 $236/d → L5 $6750/d; $brine cost then +$reclaim bonus stacked; dailyReclaimBonus field + revenue/profit + success alert; HeaderHUD cyan bonus pill; 13 RC-tests).** Extended `ConstructionAdapter` with `reclaimedWaterBonusPerDay(liveRoSkids,flow,tariff,effluent)` pure domain + `RECLAIMED_BONUS_RATE_PER_SKID=0.15` cap 36% + thresholds TSS 2.0/path 2000/turb 2.0/tox 5. `ConstructionTickEffect` gains `reuseBonusPerDay`/`reuseBonusRate`; summary shows `+$X/d reclaim bonus (Y% × N RO)` when live. `GameManager.tick` computes bonus AFTER RO polishing (polished effluent quality-gated, basin>0 & RO live & flow>10), adds to `dailyReclaimBonus`/`dailyRevenue`/`netDailyProfit`, surfaces/clears `reclaim_bonus` success alert. `HeaderHUD` shows cyan reclaim pill alongside financing. Basin-gated like brine/reagent. 13 RC-tests pin pure helper, cap, quality gating, flow scaling, summary, tick lifecycle and no-basin/no-flow. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 42 (2026-08-27): **RO SLICE 3 — brine disposal economics (zero-liquid loop pressure).** Extended `ConstructionAdapter` with `brineDisposalOpexPerDay(liveRoSkids, poweredBrineTanks, flow)` pure domain + `RO_BRINE_DISPOSAL_COST_PER_M3_HANDLED=0.005` vs `UNHANDLED=0.022` (4.4× haulage premium; 25% recovery brine volume). Each live RO skid's brine handled 1:1 by a powered `brine_tank`; missing/extra tanks map to `handledBrineSkids/unhandledBrineSkids` + flow-scaled `brineOpexPerDay` gated on `basins>0 && live>0 && flow>10` (no basin = $0, like reagent). At L1 3500 m³/d 1 handled ≈ $17.5/d, 1 hauled ≈ $77/d; at L4 12k with 3 skids unhandled ≈ $792/d vs $180/d handled — tycoon must build brine tanks to close the loop. `ConstructionTickEffect` gains `brineOpexPerDay/handledBrineSkids/unhandledBrineSkids`; summary now shows `$X/d brine (Y handled, Z hauled)` or hauled hint when flow 0. `GameManager.tick` now charges `brineOpexPerDay` as 100% sludge/disposal (`dailySludgeDisposalCost += brine`, `dailyOpex += brine`) alongside reagent, and surfaces/clears a `brine_haulage` warning alert when premium haulage is live (`Brine haulage: N skid(s) hauled … add N powered Brine Holding Tank(s) …`). `ProcessRecognition` Tertiary RO badge now amber haulage vs cyan zero-liquid: no brine or partially hauled → `Tertiary RO — haulage` amber with `hauled off-site — add N powered brine tank(s)` detail; fully handled → `Tertiary RO` cyan `zero-liquid loop` detail. 16 new BR-tests BR01–BR11 pin pure helper 0/linear/4.4×/stack/L4 scaling, evaluate identity/no-basin/no-flow, summary handled/hauled/mixed, badge amber vs cyan, and tick financial/alert lifecycle (sludge delta ≈ brine, OPEX cheaper when handled, alert present/cleared, profit gap, no-flow identity, 2+1 amber vs 2+2 cyan). Gates: build ✅ tsc ✅ sim ALL PASS (BR 11/11 + 16 new) ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 41 (2026-08-27): **RO SLICE 2 — tertiary RO live physics (powered skids polish to potable).** Extended `ConstructionAdapter.evaluateConstructionEffects` with RO tertiary stage: each powered `ro_skid` on open ground (needs power_cable) polishes effluent when basins>0 — per-skid multipliers 0.18x TSS/turbidity, 0.32x TP, 0.72x BOD, 0.75x COD, 0.82x TN, 0.35x toxicIndex, 0.08x pathogens (~1.1 log), flooring at TSS 0.01/TP 0.08/toxic 0.08/pathogens 0.003/BOD 0.45/COD 0.50/TN 0.60 with stacking (2 skids ~0.032x TSS/0.006x pathogens, 3 skids potable-grade); brine handling tracked (totalBrine/poweredBrine) but badge-only this slice (future: recovery vs brine-capacity). `ConstructionTickEffect` gains `totalRoSkids/poweredRoSkids/liveRoSkids/totalBrineTanks/poweredBrineTanks` + `toxicMultiplier/pathogensMultiplier`. `GameManager.tick` now checks `toxic/pathogens !==1` in effChanged gate and multiplies `toxicIndex`/`pathogens` alongside existing BOD/COD/TSS/TN/TP/turbidity (clamped ≥0). Summary adds `N RO skid(s) polishing` chip when basins>0. 13 new ROP-tests ROP01–ROP12 pin identity/no-RO, single-skid TSS/TP/toxic/pathogens/COD/TN, two-skid squares, three-skid floors (with volume-bonus nuance), unpowered identity, no-basin identity, brine-agnostic, tick lifecycles (TSS 182→33, TP 5.5→1.05, pathogens 507k→40k, toxics near-zero handling), chem+RO stacking (< either alone), summary RO chip, and counts. Gates: build ✅ tsc ✅ sim ALL PASS (ROP 13/13) ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 40 (2026-08-27): **RO SLICE 1 — tertiary reverse-osmosis kit (placement + 3D visuals + telemetry).** Extended `ProcessEquipment` with `ro_skid` ($28.5k/12 kW ground, 4×8″ spiral-wound vessels + HP pump on skid) + `brine_tank` ($13.5k/1.5 kW ground, bunded brine holding tank) — both `ground` (dry-installed, one per tile, blocked inside basins, 70% salvage, cash-gated). `ConstructionNetwork` now tracks `totalRoUnits/poweredRoUnits` per-type `totalRoSkids/poweredRoSkids/totalBrineTanks/poweredBrineTanks` + livePower/liveOpex + `RO live` summary line. `SceneManager.buildEquipmentMesh` adds two distinctive meshes: 4-vessel RO skid (white vessels + blue caps + HP pump + manifold + PLC light) and brine tank (bunded cylinder + dark brine surface + vent + discharge stub) with cyan/amber shimmers when powered vs red when dark (amber selection wins). `BuildToolbar` EQUIP_ICONS adds Filter/Cylinder (toolbar now 13 EQUIP buttons). `ConstructionStatusChip` new sky/amber RO chip shows `x/y RO skid(s) live` + brine counts. `EquipmentInspector` adds RO/brine live copy. `ProcessRecognition` gains 12th badge `Tertiary RO` (cyan, ≥1 powered RO skid; `brine ready` when brine missing, `Brine handling`/`RO dormant` variants) — tertiary observability without physics. 16 new RO-tests RO01–RO15 pin ground mounting, tile exclusivity, exact CAPEX ($28.5k/$13.5k) + cash gate, unaffordable rejection, powered model + livePower (4+12+1.5), basin integrity (ground RO does NOT block basin demolish, unlike in-basin kits), 70% salvage, badge thresholds (no basins → no badge, powered vs dormant, skid+brine detail, brine-alone), summary line, and unknown-type guard. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 39 (2026-08-27): **TYCOON POLISH — municipal overdraft financing (tycoon financing pressure).** NEW `OVERDRAFT_ANNUAL_RATE=0.18` (18% APR) / `OVERDRAFT_DAILY_RATE=0.18/365≈0.000493` + pure `overdraftFinancingCostPerDay(cash)` (0 when cash>=0, -cash*rate; guards NaN/Infinity) in `GameManager.ts`. `GameFinancials` gains `dailyFinancingCost` (USD/day) — initialized 0 in `SimulationEngine` (net = revenue - OPEX - fines - financing) and `GameManager.createInitialState`. `GameManager.tick` now charges financing when campaign cash<0 (sandbox free): `dailyFinancingCost += -cash*rate`, `netDailyProfit -= financing`, push `overdraft_financing` warning alert (18% APR copy) when >$0.01/day (surfaces once per tick; cleared when solvent — teaches players to avoid snowball debt). `HeaderHUD` cash pill now pulses rose (`bg-rose-950/40 border-rose-500/50 animate-pulse`, `text-rose-300/400`) when overdrawn; second line flips from profit to `-$X/d financing` (amber) when financing>0.5/d; hover tooltip shows full financing breakdown (`Overdraft $Y debt — $X.X/d at 18% APR` when overdrawn). Cash floor remains -$50k (max ~$24.7/d interest). 14 new domain tests OV01-OV08 pin helper zero/positive/NaN guards, linear 18% scale (-10k->$4.93/-25k->$12.33/-50k->$24.66), positive/zero vs overdrawn ticks + alerts, max floor, sandbox free, flowing-plant profit gap equals financing, and repayment clears. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 38 (2026-08-27): **V2-B STAGE DRESSING — biome-illustrated campaign stage cards (tycoon game feel).** `src/ui/LevelModal.tsx` now renders per-biome dressed headers: each of the 5 stages gets a distinct gradient (coastal sky/cyan, farmland emerald/lime, industrial zinc/stone, lake_forest emerald/teal, desert amber/orange), a 88-px watermark biome icon (Waves/Leaf/Factory/Trees/Sun at 10% opacity) + `COASTAL/FARMLAND/INDUSTRIAL/ECO-CITY/DESERT` pill matching `LevelBiome` + top-right difficulty pill, title/subtitle over a dark scrim for readability, and a body with briefing + italic backgroundStory + biome `envLabel` (“Coastal bay · Sandy beaches…” etc) + map-size, 4-objective preview chips + economy strip (Budget/Tariff/Reward). Header copy now reads “Each stage is a distinct environment — story, terrain & treatment challenge” and subtitle “Campaign Levels — five distinct worlds”. Pure UI (single file, no sim change). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅ — 5 stages each visually distinct, addressing V2-B “DISTINCT surrounding environment” directly.
- iter 37 (2026-08-27): **TYCOON POLISH — reagent OPEX / flow-scaled chemical consumption (tycoon economic pressure).** NEW `src/design/ConstructionAdapter.ts`: `CHEMICAL_DOSE_MG_PER_L=60`, `CHEMICAL_REAGENT_PRICE_PER_KG=0.55`, `CHEMICAL_REAGENT_COST_PER_M3_PER_PUMP=0.033` ($0.033/m³ per active dosing pump). At L1 (3500 m³/d) one active pump ≈ $115/d; at L4 (12000 m³/d) ≈ $396/d. `chemicalReagentOpexPerDay(activePumps, flow)` pure domain + `reagentOpexPerDay` field on `ConstructionTickEffect` (flow-gated, only when basins>0 && activeDosing>0 && flow>10). `evaluateConstructionEffects` now takes optional 5th `flowM3d` param (default 0 keeps backward compat, 0→$0). Summary adds `$X/d reagent` chip when live. `GameManager.tick` now passes `finalEffluent.flowRate` into the adapter and charges `reagentOpexPerDay` as 100% chemical OPEX (`dailyChemicalCost += reagent`, `dailyOpex += reagent`) on top of static 40/60 split — dosing in septic zones or unpowered rigs costs nothing and yields no TP. `EquipmentInspector` for dosing pumps now shows live reagent line (`Reagent $X/d at Y m³/d (60 mg/L × $0.55/kg) — scales with flow & pumps`) or pre-flow hint, fed via new `flowM3d` prop from `App.tsx`. 14 new domain tests RE01–RE08 pin pure cost linearity (0/1/2 pumps, 0 flow, 3500 vs 12000), evaluate with/without flow, dormant septic, storage-alone free, baffled per-zone counting, summary inclusion, and tick financial lifecycle (chemical+opex uplift ≈ reagent, netProfit reduced). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 36 (2026-08-27): **TYCOON POLISH — owner-builder construction contracts (tycoon economy pressure).** NEW campaign objective engine: 4 flow-INDEPENDENT construction contract ids (`obj_custom_basins` basins≥target, `obj_custom_baffles` baffles≥target, `obj_custom_equipment` machines≥target, `obj_custom_powered` poweredCount≥target) — checked BEFORE the `hasEffluentFlow` gate so players can earn tycoon progress by building even before the plant flows, teaching “build first, then treat”. Handlers live in `GameManager.tick` (powered check reuses `_poweredIds` single source of truth; passive diffusers always count as powered). `LevelsData` L4 (Emerald Lake Eco-City, BNR stage) gains showcase contract `obj_custom_basins` target 2 — narrative: excavate 2 custom basins (or baffled zones) for staged anoxic→aerobic BNR, proving you can build the process instead of placing prefab A2O. Campaign completion now respects construction contracts (all-met latches). 8 new domain tests CC01–CC08 pin 0/1/2 basins, default target, baffles 2, equipment 3, powered 0/1/2 (passive diffuser counts), flow-independence (no-pipe plant still passes basins), Level 4 objective presence, and completion latching (construction alone insufficient until BOD also met). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 35 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 7 slice 3 — chemical dosing kit (storage + dosing pump; lime visuals + TP polish).** Extended `ProcessEquipment` with `chemical_storage_tank` ($11.5k/0.6 kW ground) + `chemical_dosing_pump` ($6.8k/0.9 kW in_basin) — tile-exclusive, dry/wet mounting, 70% salvage, cash-gated; `ConstructionNetwork` now tracks `totalChemicalUnits/poweredChemicalUnits` per-type storage/dosing + livePower/OPEX + lime summary line; `ConstructionAdapter` adds `tpMultiplier`: active dosing pump in healthy zone = 0.78× TP each + storage 0.92× each, cap 0.35, basin-gated; `chemicalLiveSets()` exposes poweredStorage/activeDosing ids; `SceneManager` renders bunded tank + kiosk and dosing skid day-tank + pump + lance (lime shimmer when active vs red/amber when dormant, amber selection wins); `BuildToolbar` adds FlaskConical/Beaker (11 EQUIP buttons); `ConstructionStatusChip` adds lime/amber chemical chip; `EquipmentInspector` shows lime/dormant chemical copy + zone chip; `ProcessRecognition` adds 11th badge `Chemically dosed` (lime, ≥1 active dosing in mixed zone) and dormants; `GameManager.tick` multiplies TP via `(tss*0.5+0.5)*tpMultiplier` and includes tp in effChanged. 16 new domain tests CH01–CH16 pin mounting, exclusivity, CAPEX, powered model, livePower, basin integrity, badge thresholds, TP multipliers (0.78/0.92/stack/cap), tick TP lifecycle (4.30 < 5.58), salvage, summary and baffled zones. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 34 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 7 slice 2 — instrumentation kit (process sensors: DO probe + flow meter + level transmitter).** Extended `ProcessEquipment` with `do_probe` ($3.2k/0.3 kW in_basin) + `flow_meter` ($7.5k/0.45 kW ground) + `level_sensor` ($4.8k/0.25 kW in_basin) — wet/dry mounting mirrors diffusers/pumps, tile-exclusive, 70% salvage, cash-gated; `ConstructionNetwork` now surfaces `totalSensors/poweredSensors` per-type + livePower/OPEX + summary line; `SceneManager` renders 3 distinctive meshes (DO probe shaft+tip+transmitter, mag-flow spool flanges+teal box, pole+horn ultrasonic at basin-depth scale) with teal shimmer when powered vs red when dark (amber selection wins); `BuildToolbar` adds Activity/Gauge/Ruler icons (9 EQUIP buttons); `ConstructionStatusChip` adds teal/amber sensor chip; `EquipmentInspector` shows live/dark telemetry copy + zone chip; `ProcessRecognition` adds 10th badge `Instrumented` (slate, ≥2 live sensors, `full triad` when all 3 types live, read-only observability lens). 16 new domain tests IN01–IN16 pin wet/dry mounting, exclusivity, exact CAPEX, funding, powered vs dark sensor stats/power, basin integrity, Instrumented thresholds/triad/unpowered, salvage, summary and baffled zones. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 33 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 7 slice 1 — emergent process recognition (descriptive badges).** NEW `src/design/ProcessRecognition.ts` pure domain `recognizeProcess(...)` reads the live construction network (basins+baffles+equipment+utilities) and emits 9 read-only badge types — Compartmentalised, Aerated, Mixed, Activated-sludge-like, Anoxic→Aerobic, Membrane barrier (MBR-like live/fouled), Biofilm media (MBBR-like active/dormant), Hybrid IFAS-like, Septic risk — each with tone (violet/emerald/sky/cyan/amber) and one-line detail, without touching physics. NEW `src/ui/ProcessBadgeStrip.tsx` HUD strip (top 92px, 11px mono pills, lucide icons) rendered beneath ConstructionStatusChip as the second-glance process lens. `App.tsx` wires `recognizeProcess(...)` directly into the HUD. 16 new domain tests PR01–PR16 pin empty identity, aerated/mixed/activated co-location, baffled compartment count, anoxic→aerobic vs fully-aerated negative, live vs septic-fouled vs unpowered membranes, active vs dormant carriers, IFAS hybrid, septic incomplete mixing, and summary-line joining. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 32 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 6 slice 2 — filtration physics (per-zone membrane & carrier live effects).** Extended `ConstructionAdapter.evaluateConstructionEffects` with a zone-scoped filtration pass: derived zones (via allZones when baffled, basin-as-zone otherwise) build healthy/aerated maps (powered mixer / aerated diffuser per zone); each powered membrane in a healthy zone counts as live (0.20× TSS, 0.95× BOD / 0.96× COD), in a septic zone as degraded (0.55× TSS, 0.98× BOD, fouling hint); each carrier in a healthy zone counts as active (0.965× BOD, 0.97× COD) and if that zone also holds an aerated diffuser as aerated (extra 0.97× BOD + 0.985× TN). Stacking capped (TSS floor 0.02, BOD 0.70, TN 0.88, COD 0.74) after volume/aeration and before septic, so septic still penalises a filtered train honestly. NEW `filtrationLiveSets()` exposes live/degraded/active/aerated id sets for rendering. `SceneManager.syncEquipment` gains a 6th `filtration` param and tints membranes cyan (live), amber (degraded/fouled) or red (unpowered), and carriers cyan (aerated), sky (active) or dim (dormant) — selection amber still wins. `EquipmentInspector` gains filtrationLive/Degraded + carrierActive/Aerated props with distinct copy (Filtering vs Fouled vs Biofilm aerated/active/dormant) and retains the zone chip. `App.tsx` now passes `filtrationLiveSets(...)` to all 16 syncEquipment call-sites and feeds live sets to the inspector. 13 new domain tests FM16–FM28 pin identity without kit, healthy vs septic membrane TSS (0.195 vs 0.569 + live/degraded sets), unpowered identity, carrier active vs dormant BOD, aerated extra BOD/TN (0.872 vs 0.936), multi-membrane floor 0.015 and multi-carrier floor 0.65, baffled per-zone live vs dormant, degraded set, and tick lifecycles (membrane tick TSS 1772→345 <50% and carrier BOD 101.7→90.8 with aeration). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 31 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 6 slice 1 — in-basin filtration kit (placement + 3D visuals).** Added `membrane_cassette` ($18.5k, 5 kW powered) + `mbbr_carrier` ($6.8k passive) to `ProcessEquipment` catalog — both `in_basin` like diffusers/mixers (must sit inside drawn basins, one per tile, symmetric basin/legacy blocking, mounting integrity on basin demolish). `ConstructionNetwork` now tracks `totalMembranes/poweredMembranes/totalCarriers` and 5 kW live draw. `SceneManager.buildEquipmentMesh` renders a white cassette housing + five hollow fibers with header/stub for membranes, and a 7-wheel sky-blue carrier heap + wireframe cage at mid-depth for media (scaled to basin depth). `BuildToolbar` auto-expands to 6 EQUIPMENT buttons (ShieldCheck/Hexagon icons), `EquipmentInspector` shows filtration-specific live copy + zone chip, `ConstructionStatusChip` adds cyan membrane/carrier chip. 15 new domain tests FM1–FM15 (mounting in_basin vs ground, tile exclusivity, exact campaign CAPEX, unaffordable rejection, powered model membrane needs cable — wired to empty basin tile so 5 kW isolated — carrier always live, stats power accounting, baffled per-zone membership gridI 0 vs 1, 70% salvage, basin integrity). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 30 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 5 slice 2 — zone-scoped effects & membership (per-zone septic wired into the live sim).** `ConstructionAdapter` now accepts optional `baffles` param: when baffles exist it derives zones via `allZones` and counts `healthyZones/septicZones` per zone (each zone needs a powered mixer), vs legacy per-basin fallback when `[]`. Per-zone penalty 1.045/zone BOD (vs 1.08/basin) capped at 6 zones, DO −0.22/zone, with 1.35/1.25/1.30 caps; zero-baffles identity preserved. `GameManager.tick` now passes `customBaffles`, zone-aware septic alert uses "zone(s)" label when baffled. App: `EquipmentInspector` gains host-zone role chip + per-zone mixing hint; basin inspect toast shows healthy/septic zone breakdown. 8 new domain tests Z12–Z18 (single-zone split health, fully-mixed twin, fallback identity, 6-zone grid, zone-aware tick alert lifecycle, zoneForEquipmentItem gridI, empty-world identity) pin zone-scoped vs legacy parity. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 29 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 5 slice 1 — zones & baffles (basin compartmentalisation: placement + 3D visuals).** NEW `src/design/BasinZone.ts` pure domain: `BaffleWall` (vertical/horizontal full-span interior walls, offset 1..dim-1, validate + $55/m²+450 CAPEX) + `BasinZone` derived grid (Cartesian product of sorted V/H offsets → cells, roles anoxic/aerobic/buffer, zoneVolume, pointNearBaffle). `GameManager` adds `customBaffles[]`/`selectedBaffleId` + `placeBaffle`/`demolishBaffle` (60% salvage, sandbox $0, duplicate guards) + `zonesForBasin`/`allZones`/`zoneAtTile`/`baffleAtPoint`/`basinZoneStats` + basin demolish cascades baffles. `SceneManager.syncBaffles` + `setBafflePreview` (thin concrete wall 0.18 m, violet #c4b5fd, purple emissive on select, lilac dashed preview). App: `ToolMode 'draw_baffle'` + `BuildToolbar` BAFFLES group (│ Vertical / ─ Horizontal, violet), one-click baffle inside basin (ghost validity + dashed preview), select/inspect/demolish (baffle-first hit-test, 60% refund), Esc + undo/level sync, HUD `ConstructionStatusChip` zones/baffles chip (violet), NEW `BaffleInspector` panel. 19 new domain tests Z1–Z11 (single zone, vertical/horizontal split, 2V+1H grid, validation bounds/duplicate, CAPEX determinism, allZones, stats, pointNear, GameManager placement/funds/zones/baffleAtPoint/demolish/zone-membership). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 28 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 4 slice 2 — thin adapter into the legacy sim (live process effects).** NEW `src/design/ConstructionAdapter.ts` pure domain `evaluateConstructionEffects` (aerated diffusers → BOD 3%/TN 1.5% polish, basin volume 3–4% settling, septic penalty BOD+8%/DO−0.4 per unmixed basin, livePower/liveOpex) + `GameManager.tick` thin-adapter block (effluent×multipliers + power/OPEX added at $0.15/kWh + permit re-evaluated + septic warning `construction_septic`). Zero construction = zero effect. 13 new tests CA0–CA6 (identity, aerated BOD 5.1→4.7 & DO lift, unaerated, septic vs healthy, power gate 15 kW/$30, tick alert lifecycle, aeration cap 8→16). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 27 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 4 slice 1 — construction network (live power & aeration).**
  NEW `src/design/ConstructionNetwork.ts` pure domain: `poweredEquipmentIds` (powerKw>0 needs incident power_cable, passive diffuser always live), `aeratedDiffuserIds` (air_pipe diffuser↔blower only when blower powered), `constructionStats` (basin volume/area, powered/aerated counts, live kW). `GameManager` wrappers + `SceneManager.syncEquipment` visual tints (red unpowered, blue aerated) + `ConstructionStatusChip` HUD (basins · powered · aerated · utilities) + `EquipmentInspector` panel (live badge + remove) + App wiring for all sync/undo/level/Esc paths + 11 N-tests. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 26 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 3 landed — utility connections.**
  NEW `src/design/UtilityConnection.ts`: three utility types (water_pipe $185/m+900
  air_pipe $110/m+600 power_cable $65/m+400) with TYPE-SPECIFIC host rules —
  water needs pump/basin, air is strict blower↔diffuser, power needs powered kit
  (pump/mixer/blower); Euclidean length (tile*6m) + fixed tie-in cost;
  distinct endpoints, bounds, host, duplicate guards. `GameManager`
  `placeUtilityConnection`/`demolishUtilityConnection` (campaign 60% salvage,
  sandbox $0, cascade on basin/equipment removal, atomic cash gate) +
  `utilityAtPoint`/`utilitiesAtTile` hit-testing (midline proximity 0.65 tiles).
  GameState `utilityConnections[]` + `selectedUtilityId`. SceneManager
  `syncUtilityConnections` (color-coded Lines: water #3b82f6 / air #f97316 /
  power #eab308 + endpoint dot markers + white selection highlight +
  LineDashedMaterial dashed preview) + `setUtilityPreview`. BuildToolbar:
  new `connect_utility` ToolMode + UTILITY group (Water/Air/Power) with
  per-type $/m + blurb tooltips; Basin button now clears utility selection.
  App: `connect_utility` two-click flow (source host → target host, same-tile
  cancel, host validation, duplicate guard, cost toast), right-click + Esc
  `cancelUtilitySelection`, hover ghost validity + dashed preview line,
  select (click line → inspect, white highlight) / demolish (click line →
  cut, 60% refund) with utility-first hit-testing, demolish cascades for
  basin+equipment, undo/redo syncs utilities, initial load syncs utilities.
  23 new domain tests U1–U23 pin all type rules, cost, duplicate, bounds,
  funding, demolish/salvage, cascade, and hit-testing. Gates: build ✅ tsc ✅
  sim ALL PASS (U1-23) ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 25 (2026-08-27): **CONSTRUCTION-BUILDER PHASE 2 landed — physical
  equipment placement.** NEW `src/design/ProcessEquipment.ts`: starter
  catalog (fine_bubble_diffuser $4.2k / submersible_mixer $9.8k /
  process_pump $14.5k / rotary_blower $32k) with MOUNTING RULES —
  in_basin types must sit inside a player-drawn basin (wet-installed),
  ground types must sit on free open ground outside all basins and legacy
  lots (dry-installed); one machine per tile; bounds checks.
  `GameManager.placeProcessEquipment` / `demolishProcessEquipment`
  (cash-gated, sandbox-aware, catalog CAPEX, 70% kit salvage; tutorial $0),
  `equipmentAtTile` hit-test. SYMMETRIC BLOCKING completed both directions:
  ground equipment blocks legacy placeUnit lots AND basin drawing;
  mounting integrity refuses demolishing a basin that still holds machines.
  GameState gains `processEquipment[]`. SceneManager.syncEquipment renders
  recognizable 3D machines (diffuser grid panel + domes on basin floor,
  mixer base/shaft/propeller scaled to basin depth, pump plinth+volute+
  motor, twin-tank blower skid) with add/remove/dispose discipline and amber
  selection highlight. App: new `place_equipment` tool mode, orange toolbar
  EQUIPMENT group (4 buttons), single-tile valid/invalid ghost, install/
  inspect/demolish flows with cost toasts. 23 new domain tests E1–E23.
  Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 62/62+static ✅ eng 368/368 ✅.
- iter 24 (2026-08-27): **MBR migration SLICE 4 landed — MBR §Q/§R migration COMPLETE** — cleaning now has a throughput cost and membranes die of old age. (a) CIP OUTAGE: `performMembraneClean` sets `offlineHours = 6` (`MBR_CIP_OFFLINE_HOURS`, drain→soak→rinse→refill); `advanceMbrFouling` counts it down per tick and pro-rates reversible growth/backwash/irreversible aging by the online fraction; `calculateUnitProcess` bypasses ALL permeate+WAS production while offline at 25% standby power — a clean now costs its fee AND ~6 h of zero output, so players schedule cleans for low-flow hours. (b) END OF LIFE: `MbrFoulingState` gains `ageDays` + `endOfLife`; irreversible (permanent-aging) resistance accumulates daily ∝ foulDriver (`MBR_IRREVERSIBLE_RATE_PER_DAY = 0.0003`, calibrated so gentle duty lives out §R lifetimeYears via the AGE trigger while heavy duty kills polymers early via the FOULING trigger `MBR_EOL_IRREVERSIBLE = 1.5`). NEW `membraneReplacementCostUsd` = area × §R catalog capexUsdPerM2 (ceramic > PVDF > PES); domain action `GameManager.replaceMbrMembranes` charges outside sandbox atomically and swaps cassettes for brand-new (resistance/age/EoL/outage all reset). AdvisoryEngine emits critical `mbr_replacement_due_*` with one-click `replace_mbr` fix (coexists with clean-due); App.tsx routes OperatorConsole + new UnitDesigner Replace button through it. Diagnostics shows CIP-outage countdown, cassette age vs rated life, EoL flag. 26 new eng tests MBR32–49 pin window countdown/pro-rating/frozen-offline physics, both EoL triggers, quote math, charge-once debit, atomic rejection, sandbox path, runtime bypass flows, and advisory cases. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+62/62 ✅ eng 368/368 ✅.
- iter 23 (2026-08-26): **MBR migration SLICE 3 landed** — CIP cleaning now costs real money and announces itself. NEW `membraneCipCostUsd` (+ `MBR_CIP_COST_USD_PER_M2 = 2.4`) in `MBR.ts`: deterministic quote = installed area × basis × material reagent factor (0.8+0.4·foulingCoeff ⇒ PES > PVDF > ceramic). NEW domain action `GameManager.cleanMbrMembranes(state, unitId)` (setUnitCommissioning pattern): affordability-gates outside sandbox, charges once, applies performMembraneClean atomically. App.tsx routes BOTH the designer's Clean button (`onUpdateFouling`) and a new OperatorConsole `clean_mbr` fix through it, with charge toasts. AdvisoryEngine emits a `mbr_clean_due_*` warning (one-click CIP fix, affordability-aware, fires regardless of flow) whenever resistance crosses the threshold. UnitDesigner button shows the live quote. Launcher regenerated per V2-C (was stale vs iter 22 code). 13 new eng tests MBR21–31 pin quote math, charge-once debit, atomic insufficient-funds rejection, sandbox-free path, non-membrane refusal, and all three advisory cases. Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 70/70+60/60 ✅ eng 342/342 ✅.
- iter 22 (2026-08-26): **MBR migration SLICE 2 landed** — runtime membrane fouling progression. Replaced the legacy binary fouled/not-fouled heuristic in `mbr_membrane` with a continuous resistance model: `MbrFoulingState` (R×, daysSinceClean, irreversible, cleaningDue) seeded on first engineered tick, advanced ONCE per sim-day by `SimulationEngine` (outside the relaxation loop, so it can't be multiplied by solver passes), persisted on the unit. `evaluateMbrRuntime` degrades flux/TMP/power/opex as R climbs (TMP ∝ flux ÷ (permeability/ρ)); the absolute barrier (permeate TSS≈0, 4-log) holds regardless. CIP cleaning (`performMembraneClean`) strips resistance + resets the clock; wired to a "Clean Membranes" button in the MBR Diagnostics tab via a new `onUpdateFouling` callback → `App.tsx`. Show-Calculation block renders live resistance/TMP/headroom/power/opex. 10 new eng tests MBR11–20; ui-interaction gained an MBR row (TMP eq + resistance readout + CIP control). Gates: build ✅ tsc ✅ sim ALL PASS ✅ ui 60/60 ✅ eng 329/329 ✅.

## Notes
- Never delete files — move into junk/. Probes live in junk/autopilot-20260826/ (delim-scan, probe-range-events, probe-input-death, probe-input-matrix, probe-checkbox-delegation document the React‑event investigation); scratch gate‑run logs in junk/autopilot-20260827/.
- HEAD baseline comparison trick: git worktree in $LOCALAPPDATA/Temp with a node_modules junction; prune metadata afterwards. (Used stash round‑trip in iter 8 — equally effective for tracked‑file baselines.)
## ⭐ MISSION DIRECTIVE v2 (user, 2026-08-26 evening) — SUPREME PRIORITY
User has set the long-term product direction. Work everything below through the
existing top-down order (finish backlog #1 eng-test physics gaps first, then
Section A, then Phase-1 audit) — but EVERY slice must advance this direction:

### V2-A. FULLY DESIGNABLE UNITS (the core of the game)
Every unit in the game must become engineer-designed, not predefined-box-placed:
dimensions (L×W×H / Ø×SWD), construction materials, equipment selections,
operation parameters — each with proper engineering calculations behind it
(HRT, SOR, SLR, flux, headloss, duty points, MLSS/SRT, quantities → CAPEX).
Extend the src/design/* blueprint+geometry+validator system until ALL unit
families are designable. Predefined units remain only as templates/presets.
EXPAND THE CATALOG with new unit types not yet in the game (e.g. DAF,
anaerobic digesters variants, constructed wetlands, cyclonic grit, drum
screens, SBR, IFAS, MBBR carriers, UV/ozonation chambers, sludge dryers,
biogas CHP, chemical storage/dosing, outfall structures…).

### V2-B. TYCOON GAME FEEL — STORY, ART, STAGES
Make it an enjoyable tycoon, not just a sandbox calculator:
- Stages/campaign levels each get a DISTINCT surrounding environment
  (different terrain themes, climates, city contexts) and DIFFERENT GOALS.
- Build story/narrative per stage: briefs from characters/municipalities,
  progression arcs, stakes.
- Add illustrations/art where feasible (UI portraits, event art, scene dressing).
- Economic depth: contracts, reputation, financing, OPEX pressure, expansions.

### V2-C. LAUNCHER (DONE by user's session 2026-08-26)
package.json build now chains make-launcher.mjs — AquaTycoon_Launcher.html
auto-regenerates on every npm run build. NEVER remove this chain. The
launcher HTML at repo root is tracked in git and must always reflect HEAD.
